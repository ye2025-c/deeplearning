# 自然语言处理：应用

> 对应章节：`pytorch/chapter_natural-language-processing-applications/`
> 阶段：4（序列模型、注意力与 NLP）
> 状态：⬜ 待填
> 过关标准：能用预训练词向量/BERT 跑通一个情感分析或文本分类任务（端到端）。
> 关联：`NLP预训练.md`、`现代循环网络.md`、`卷积神经网络.md`

---

## 1. 情感分析数据集（IMDb）

**Q：文本分类的数据预处理流程？变长序列如何对齐成批？**

要点清单：
- [ ] 分词 → 词表 → 截断/填充到固定长度
- [ ] DataLoader 产出 `(tokens, labels)`

> TODO

---

## 2. 情感分析：用 RNN（双向 LSTM）

**Q：为什么用双向 RNN 做整句情感分类？最后怎么聚合成一个句向量？**

```python
# Embedding(用预训练词向量初始化) -> BiLSTM -> 取首尾隐状态拼接 -> Linear(2 类)
# TODO
```

---

## 3. 情感分析：用 CNN（textCNN）

**Q：一维卷积如何捕捉「n-gram 局部特征」？时序最大汇聚（max-over-time pooling）在做什么？**

要点清单：
- [ ] 多种卷积核宽度并行（捕捉 2/3/4-gram）
- [ ] 每个通道做 max-over-time → 拼接 → 全连接分类

```python
# Embedding -> 多组一维卷积 -> ReLU -> max-over-time pooling -> 拼接 -> Linear
# TODO
```

---

## 4. 自然语言推断（NLI）数据集（SNLI）

**Q：自然语言推断在判断什么（蕴含/矛盾/中立）？输入是「一对句子」如何处理？**

> TODO

---

## 5. 自然语言推断：用注意力（可分解注意力模型）

**Q：注意（Attend）→ 比较（Compare）→ 聚合（Aggregate）三步分别做什么？**

> 与 `注意力机制.md` 对照：这是注意力在「句子对匹配」上的应用。
> TODO

---

## 6. 微调 BERT ⭐

**Q：序列级任务（分类）和词元级任务（标注/问答）分别取 BERT 的哪部分输出？为什么微调比从零训练高效？**

要点清单：
- [ ] 序列级：取 `<cls>` 输出 → 接一个分类头
- [ ] 词元级：取每个词元的输出 → 序列标注 / 抽取式问答
- [ ] 微调：加载预训练权重，小学习率整体更新

```python
# 1. 加载预训练 BERT
# 2. 数据转成 BERT 输入（tokens + segment + valid_len）
# 3. <cls> 输出 -> Linear 分类头
# 4. 小 lr 微调
# TODO
```

> 这是把 `NLP预训练.md` 里 BERT 用起来的落地章，建议两篇对照写。

---

## 任务-模型对应速查

| 任务 | 典型模型 | 取哪部分表示 |
|------|----------|--------------|
| 情感分析 | BiLSTM / textCNN / BERT | 句向量 |
| 自然语言推断 | 可分解注意力 / BERT | 句子对 |
| 序列标注 | BERT | 每个词元输出 |
| 抽取式问答 | BERT | 起止位置 |

---

## 踩坑记录（问题 + 原因 + 改法）

-
