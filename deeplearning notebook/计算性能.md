# 计算性能

> 对应章节：`pytorch/chapter_computational-performance/`
> 阶段：穿插学习（偏工程，按需）
> 状态：⬜ 待填
> 过关标准：理解命令式/符号式编程的取舍，能把单机训练改造成多 GPU 数据并行。
> 关联：`learning_process/Timer.py`、`learning_process/矢量加速.py`

---

## 1. 编译器和解释器（命令式 vs 符号式）

**Q：命令式编程（易调试）和符号式编程（易优化/部署）各有什么取舍？`torch.jit` 在做什么？**

要点清单：
- [ ] 命令式：逐行执行，方便 debug
- [ ] 符号式：先构图再执行，利于优化和导出
- [ ] PyTorch 的混合：`torch.jit.script` / `trace`

> TODO

---

## 2. 异步计算

**Q：PyTorch 的 GPU 运算是异步排队的，这对「计时」有什么坑？**

> 提示：计时 GPU 操作前要 `torch.cuda.synchronize()`，否则测的是排队时间。
> 对照 `learning_process/Timer.py`。
> TODO

---

## 3. 自动并行

**Q：不同设备上的无依赖运算如何自动并行？计算与通信能否重叠？**

> TODO

---

## 4. 硬件基础

**Q：CPU / GPU / 内存 / 存储的速度层级？为什么「数据搬运」常是瓶颈？**

要点清单：
- [ ] 内存带宽 vs 计算吞吐
- [ ] GPU 适合大规模并行（SIMT）
- [ ] 向量化、缓存友好的访存

---

## 5. 多 GPU 训练（数据并行）⭐

**Q：数据并行的流程（拆分小批量 → 各 GPU 算梯度 → 汇总 → 同步）？参数为什么要 broadcast？**

```python
# 从零：把 batch 切到多个 GPU，各算梯度，allreduce 求和，再同步参数
# 简洁：nn.DataParallel 或 DistributedDataParallel(DDP)
# TODO
```

要点清单：
- [ ] 数据并行 vs 模型并行
- [ ] 梯度同步（allreduce / ring synchronization）
- [ ] `DistributedDataParallel` 比 `DataParallel` 更推荐

---

## 6. 参数服务器（选读）

**Q：大规模分布式训练里参数服务器的角色？环同步（ring allreduce）为什么高效？**

> TODO

---

## 踩坑记录（问题 + 原因 + 改法）

-
