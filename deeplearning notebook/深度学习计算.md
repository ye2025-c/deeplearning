# 深度学习计算

> 对应章节：`pytorch/chapter_deep-learning-computation/`
> 阶段：2（多层感知机与正则化）
> 状态：⬜ 待填
> 过关标准：理解「层与块」的抽象，会自定义层/块、管理参数、读写模型、把张量搬到 GPU。

---

## 1. 层和块（Module）

**Q：`nn.Module` 为什么既能表示一层，也能表示整个网络？`forward` 的作用？**

要点清单：
- [ ] 自定义块：继承 `nn.Module`，实现 `__init__` 和 `forward`
- [ ] `nn.Sequential` 的本质
- [ ] 在 `forward` 里写控制流（if / 循环）

```python
class MLP(nn.Module):
    def __init__(self):
        super().__init__()
        # TODO
    def forward(self, X):
        # TODO
        pass
```

---

## 2. 参数管理

**Q：如何访问/初始化/共享参数？**

要点清单：
- [ ] `net.state_dict()` / `named_parameters()`
- [ ] 内置初始化（`nn.init.normal_` 等）与自定义初始化
- [ ] 参数绑定（共享同一层）

---

## 3. 自定义层

**Q：带参数和不带参数的自定义层分别怎么写？**

```python
class CenteredLayer(nn.Module):
    def forward(self, X):
        # TODO: 返回去均值后的 X
        pass
```

---

## 4. 读写文件（保存 / 加载）

**Q：保存张量、保存模型参数（`state_dict`）、保存整个模型的区别？**

```python
# torch.save / torch.load
# net.load_state_dict(torch.load('model.params'))
# TODO
```

> 注意：推理前要 `net.eval()`；恢复训练要同时保存 optimizer 状态。

---

## 5. GPU

**Q：如何查询 GPU、把张量/模型搬到 GPU？为什么数据和模型要在同一设备？**

```python
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
# X = X.to(device); net = net.to(device)
# TODO
```

---

## 踩坑记录（问题 + 原因 + 改法）

-
