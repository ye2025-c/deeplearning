# 计算机视觉（选读）

> 对应章节：`pytorch/chapter_computer-vision/`
> 阶段：3（卷积神经网络与计算机视觉）
> 状态：⬜ 待填（按兴趣选章，不必全做）
> 过关标准：能用数据增广 + 微调跑通一个迁移学习任务。

---

## 1. 图像增广（Data Augmentation）

**Q：数据增广为什么能提升泛化？常用变换有哪些？**

要点清单：
- [ ] 翻转、裁剪、颜色抖动（`torchvision.transforms`）
- [ ] 只在训练集做增广，测试集一般只做必要的 resize/normalize

> TODO

---

## 2. 微调（Fine-tuning）⭐

**Q：迁移学习的直觉是什么？为什么输出层重新初始化、其余层用小学习率？**

```python
# 1. 载入预训练模型（如 resnet18(pretrained=True)）
# 2. 替换最后的全连接层为目标类别数
# 3. 输出层用较大 lr，其余层用较小 lr
# TODO
```

> 这是性价比最高的一节，建议优先做一个小项目（如热狗识别 / 自定义二分类）。

---

## 3. 目标检测基础（选读）

**Q：边界框（bounding box）、锚框（anchor box）、IoU、NMS 分别是什么？**

要点清单：
- [ ] 边界框表示（中心宽高 vs 左上右下）
- [ ] 锚框与真实框的匹配
- [ ] 非极大值抑制（NMS）

> TODO（如不做检测可跳过）

---

## 4. 语义分割 / 风格迁移（选读）

> 仅记标题占位，按兴趣展开。

---

## 踩坑记录（问题 + 原因 + 改法）

-
