# 卷积神经网络（CNN 基础）

> 对应章节：`pytorch/chapter_convolutional-neural-networks/`
> 阶段：3（卷积神经网络与计算机视觉）
> 状态：⬜ 待填
> 过关标准：能口头解释卷积、填充、步幅、池化的作用，能从零复现 LeNet 并在 Fashion-MNIST 跑通。

---

## 1. 从全连接到卷积

**Q：为什么图像任务不直接用 MLP？卷积的两个核心先验是什么？**

要点清单：
- [ ] 平移不变性（translation invariance）
- [ ] 局部性（locality）
- [ ] 参数共享 → 大幅减少参数量

---

## 2. 图像卷积（互相关运算）

**Q：深度学习里的「卷积」其实是互相关，二者差别在哪？**

关键公式（二维互相关）：
$$
Y_{i,j} = \sum_{a}\sum_{b} X_{i+a,\,j+b}\, K_{a,b}
$$

```python
def corr2d(X, K):
    # TODO: 滑窗求和
    pass
```

要点清单：
- [ ] 卷积核 / 特征图（feature map）/ 感受野
- [ ] 学习卷积核（用梯度下降反推 K）

---

## 3. 填充和步幅

**Q：填充（padding）和步幅（stride）如何影响输出尺寸？**

关键公式（输出高度）：
$$
\left\lfloor \frac{n_h - k_h + p_h + s_h}{s_h} \right\rfloor
$$

> TODO：补充常见组合（如 padding=k//2 保持尺寸）

---

## 4. 多输入多输出通道

**Q：多通道输入时卷积核长什么样？$1\times1$ 卷积的作用？**

要点清单：
- [ ] 输入通道 $c_i$ → 卷积核也有 $c_i$ 个通道，按通道求和
- [ ] 输出通道 $c_o$ → 用 $c_o$ 组卷积核
- [ ] $1\times1$ 卷积 = 跨通道的全连接（调整通道数）

---

## 5. 池化层（Pooling）

**Q：最大池化 / 平均池化的作用？为什么能带来一定的平移不变性？**

```python
def pool2d(X, pool_size, mode='max'):
    # TODO
    pass
```

---

## 6. LeNet（第一个完整 CNN）

> 练手脚本：`learning_process/lenet_scratch.py`（Fashion-MNIST）

结构（自己补全维度）：
```
输入 1×28×28
→ Conv(1→6, 5×5, pad=2) → Sigmoid → AvgPool(2×2)
→ Conv(6→16, 5×5)       → Sigmoid → AvgPool(2×2)
→ Flatten → FC(16×5×5 → 120) → FC(120 → 84) → FC(84 → 10)
```

> TODO：贴训练精度 / loss 曲线，记录 GPU 加速效果

---

## 踩坑记录（问题 + 原因 + 改法）

-
